#include <stdio.h>
int main(){
    char name,hobby;
    int age;
    scanf("%s %s",&name &hobby);
    scanf("%d", &age);
    printf("Name: %s", &name);
    printf("Age: %d",&age);
    printf("Hobby: %s",&hobby);
    return 0;
}